aws_region = "specify the region of the deployment here"
ami_for_ec2 = "AMI ID for amazon linux specific to your region here"
vpc_id = "The VPC ID where the EC2 will be deployed to"
ssh_cidr = "The IP address for the source from where the ssh connection will be allowed here"